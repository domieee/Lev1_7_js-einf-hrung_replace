const text = "Sam is good at coding school";

const firstResult = text.replace("good", "bad");
const secondResult = text.replace("Sam", "Susi");
const thirdResult = text.replace("coding school", "Tennis");

document.write(firstResult + "<br>" + secondResult + "<br>" + thirdResult);